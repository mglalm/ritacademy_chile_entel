<?xml version="1.0" encoding="UTF-8" ?>
<script name="userInterface.scriptInvocaOM">
  <label>scriptInvocaOM</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="entrada" type="rifp">
      <type>dstruct_OrderManager.dsOrderProvisionInputSimple</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var rut, orderId, poIdBas, poIdAdi, ds;
    rut = entrada.rut;
    orderId = entrada.orderId;
    poIdBas = entrada.POIdBasico;
    poIdAdi = entrada.POIdAdicional;

    ds = new OrderManager.dsOrderProvisionInputSimple();
    ds.rut = rut;
    ds.orderId = orderId;
    ds.POIdBasico = poIdBas;
    ds.POIdAdicional = poIdAdi;
    return ds;
  ]]></script>
</script>