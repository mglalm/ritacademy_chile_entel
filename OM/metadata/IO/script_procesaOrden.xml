<?xml version="1.0" encoding="UTF-8" ?>
<script name="IO.procesaOrden">
  <label>procesaOrden</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="input" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var i = 1;
    var doc = new Document("Core.processDocument");
    doc.po = input.productOfferId;
    doc.orderId = input.orderId;
    Process.startProcess("Core.procesaOrden",doc,null,null);

    var dt = new DataStructure("IO.response");
    dt.productOfferId = input.productOfferId;
    dt.orderId = input.orderId;
    dt.status = "P";

    return dt;
  ]]></script>
</script>